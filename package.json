{
  "name": "nightwire",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Some of the numbers are lying — find the Ghosts at the table before the lights go out.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "vite",
    "icons": "node scripts/gen-icons.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ben-gy/game-engine": "github:ben-gy/gh-game-engine#v1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
